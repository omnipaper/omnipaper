# Public URL where omnipaper is reachable (used for auth + cookies).
APP_URL=http://localhost:3000

# Postgres password — composed into DATABASE_URL by docker-compose.
POSTGRES_PASSWORD=change-me

# Secrets — any random string, 16+ chars (e.g. openssl rand -base64 32)
AUTH_SECRET=
ENCRYPTION_KEY=
