// Contract level of the mobile/public API, advertised as [MIN_API_LEVEL, API_LEVEL] via /health.
// Bump API_LEVEL only on a breaking change; raise MIN_API_LEVEL only when dropping an old level.
export const API_LEVEL = 1;
export const MIN_API_LEVEL = 1;
