/** Public app metadata and links (no secrets). */
export const config = {
  appName: "omnipaper",
  appDescription: "Document management with native S3-compatible storage",
  links: {
    github: "https://github.com/omnipaper/omnipaper",
  },
} as const;
