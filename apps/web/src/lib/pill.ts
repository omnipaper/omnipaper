// Same height as FilterFieldChip; rounding is per call site since split buttons round edges differently.
export const pillControl = "h-auto gap-1.5 py-1.5 leading-4 [&_svg:not([class*='size-'])]:size-3.5";
